# authsvc
Authentication and authorization service for the payroll platform. Handles user accounts, password hashing (Argon2), JWT issuance and validation, and role-based access control for all tenant applications.
