version: '3.7'

services:
  api:
    image: python:3.8.2-slim-buster
    entrypoint: ./entrypoint.sh
    env_file: ./env/dev.env
    ports:
      - '5000:5000'
    working_dir: /app
    volumes:
      - ./:/app
